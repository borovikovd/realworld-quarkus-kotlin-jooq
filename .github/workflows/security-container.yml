name: "Security / Container"

on:
  push:
    branches: [master]
    paths:
      - "Dockerfile*"
      - "src/**"
      - "build.gradle.kts"
      - "gradle.properties"
  pull_request:
    branches: [master]
    paths:
      - "Dockerfile*"
      - "src/**"
      - "build.gradle.kts"
      - "gradle.properties"
  schedule:
    - cron: "0 6 * * 1"

jobs:
  trivy:
    name: Trivy
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Set up JDK 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          java-version: "21"
          distribution: "temurin"
          cache: "gradle"

      - name: Build native image
        run: chmod +x gradlew && ./gradlew build -Dquarkus.native.enabled=true -Dquarkus.native.container-build=true -Dquarkus.package.jar.enabled=false -Dquarkus.container-image.build=true -Dquarkus.container-image.tag=scan --no-daemon
        env:
          QUARKUS_CONTAINER_IMAGE_NAME: realworld-api

      - name: Trivy scan (SARIF)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: "realworld-api:scan"
          format: "sarif"
          output: "trivy-results.sarif"
          severity: "HIGH,CRITICAL"
          exit-code: "0"
          trivyignores: ".trivyignore"

      - name: Upload SARIF to GitHub Security
        if: always()
        uses: github/codeql-action/upload-sarif@e4fba868fa4b1b91e1fdab776edc8cfbe6e9fb81 # v4.37.3
        with:
          sarif_file: trivy-results.sarif
          category: trivy

      - name: Trivy scan (table)
        if: always()
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: "realworld-api:scan"
          format: "table"
          severity: "HIGH,CRITICAL,MEDIUM"
          trivyignores: ".trivyignore"
